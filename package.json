{
  "name": "messenger",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "npm run prebuild && vite build --emptyOutDir && node ./server.js",
    "prebuild": "tsc --noEmit",
    "build": "npm run prebuild && vite build --emptyOutDir",
    "lint": "eslint .",
    "stylelint": "stylelint **/*.scss"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.8",
    "eslint-config-airbnb": "^19.0.4",
    "handlebars": "4.7.8",
    "sass": "^1.70.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.8",
    "vite-plugin-handlebars": "^2.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "handlebars": "4.7.8",
    "stylelint": "^16.2.1",
    "stylelint-scss": "^6.1.0",
    "uuid": "^9.0.1"
  }
}
